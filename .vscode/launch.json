{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch server",
      "type": "node",
      "request": "launch",
      "runtimeArgs": ["-r", "ts-node/register"],
      "cwd": "${workspaceFolder}/server",
      "args": ["${workspaceFolder}/server/app.ts"]
    }
  ]
}
