{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch backend",
      "type": "node",
      "request": "launch",
      "runtimeArgs": ["-r", "ts-node/register"],
      "cwd": "${workspaceFolder}/backend",
      "args": ["${workspaceFolder}/backend/app.ts"]
    }
  ]
}
